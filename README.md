# SimpleEngineDX11
For Practicing On DX11

2017/7/31
	问题待解：
	 ·场景的渲染流程要怎么设计呢？
	 ·首先，渲染顺序是什么？
	 ·不同的物件，例如光，普通物品，带反射的，带ssr的，透明的,接受（/不接受）阴影的等等不同渲染材质的,怎么安排？
	 ·如果和延迟渲染组合起来，又应该怎么设计？

2017/8/25
	创建一个窗口对象，一个DX11设备封装对象。能简单的跑起来

2017/8/29
	直接构建整个引擎结构有点不现实。因为对API的结构和功能都不熟悉。先做一步对API的封装。这时只针对DX11。从DX11反向写起。
	
2017/8/30
	创建渲染场景，加入简单的渲染单元。

2017/9/4
	数学库基本，shader constant buffer 控制基本，摄像头基本。mesh基本。 摄像头 + mesh + shader constant 实现基本绘制。
	--carmack 上身吧，做的好慢。

2017/9/5
	引用计数原则： 1.高层引用底层，底层不引用高层
				  2.同一层的，根据创建的逻辑顺序，先创建的引用后创建的，后创建的不引用先创建的
				  3.接口部分，Createxxx 类型的，需要客户端自己手动释放。GetXXX类型的，则不需要。Setxxx类型的，根据逻辑内部AddRef或者不加，但是要注意逻辑统一。
					
	下一步要做的： 首先先简单整理一下接口，管理逻辑等，在现有的基础上修葺一下，包括SceneObj和Scene的管理。
				  加入数学库基础，摄像头类。基本控制。完成一个可以控制的摄像头和一个可描述、可保存和加载的场景。

	下下一步： 进一步的修缮框架设计，更多基础功能的加入， 加入图片支持，进一步完善材质，shader系统。